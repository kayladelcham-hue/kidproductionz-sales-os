services:
  - type: web
    name: kidproductionz-sales-os
    env: python
    buildCommand: pip install -r requirements.txt && cd app/frontend && npm install && npm run build
    startCommand: uvicorn app.api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
